# RunSmart 1.1.6 (31) Release Plan

**Status:** Planned; packaging has not started.  
**Public baseline:** 1.1.5 (30), released 2026-08-05.  
**Release source baseline:** `origin/main` at `4d81ceb` (RunSmart iOS PR #135).  
**Recommended target:** 1.1.6 (31), contingent on App Store Connect confirming that build 31 has not already been used.  
**Release objective:** ship the native auth-return repair and the complete activation measurement foundation currently merged, then obtain a trustworthy launch-to-wall cohort without adding feature scope.

## Decision

Treat 1.1.6 as a **reliability and activation-measurement release**, not a feature release.

The marketing version should move from 1.1.5 to 1.1.6 because the candidate changes real authentication behavior: an iOS-originated email/browser round trip can return to a native RunSmart session. Build 31 is the expected next build after public build 30, but must be checked in App Store Connect before editing the project.

Do not bundle guest mode, value-before-auth, Garmin commercial work, voice coach changes, monetization, localization, route redesign, or other product work into this release.

## What Has Merged Since 1.1.5

| Slice | Merged evidence | Release impact |
|---|---|---|
| Native email/auth return | PR #129, `f553561` | Universal Link and `runsmart://` callback handling can restore the native Supabase session after an iOS-originated browser round trip. Adds canonical `www.runsmart-ai.com` associated domain. |
| Internal-traffic classification | PR #130, `d49636a` | Adds `is_internal_tester` to events and identified-person properties so activation reads can exclude team/TestFlight traffic going forward. |
| Email model stability | PR #131, `3ea0a29` | Removes an isolated-deinit crash affecting the email sign-in model on back-deployed Swift concurrency runtimes. |
| First visible activation frame | PR #132, `f5fb7c7` | Emits `activation_first_frame_rendered` only after session resolution and launch-overlay dismissal. |
| Pre-auth, permission, and wall coverage | PR #135, `4d81ceb` | Covers sign-in wall, email sign-in, Terms, Privacy, HealthKit outcomes, notification failures, and guaranteed cold/warm/background `sign_in_wall_reached` events. |

No visible UI copy or layout changed in these slices. The release notes must not present analytics instrumentation as a user-facing feature.

## Current Baseline Evidence

- A clean Release simulator build from `origin/main` `4d81ceb` succeeded on 2026-08-13.
- The built app resolved to `com.runsmart.lite`, version 1.1.5, build 30; the Live Activity extension matched 1.1.5 (30).
- The built app contained non-empty PostHog and Supabase configuration values. Values were checked only for presence and were not printed.
- The exact-main full suite from WP-61a recorded 368 passed / 1 failed / 0 skipped. The only failure is the reproduced locale-dependent calendar assertion.
- Release compilation reports six existing warnings: five synchronous calls into main-actor-isolated analytics/training helpers and one iOS 17 `HKWorkout` initializer deprecation. None is introduced by this plan, but they should be resolved or explicitly accepted before archive.

## One Small Pre-Freeze Slice

Before versioning, finish WP-61a Stories 5–6 and then freeze:

1. Add the two remaining rage-click/friction observation points: post-aha and the pre-run HealthKit/permission surface.
2. Determine whether `Application Installed` can also fire on update and correct the install denominator/query if it can.
3. Record any new event birthdays and update the saved PostHog insight before packaging.

This is the only recommended scope addition. It completes the measurement packet that 1.1.6 exists to carry and avoids needing 1.1.7 solely for missing telemetry. If speed is more important than packet completeness, current `origin/main` is already sufficient to ship Stories 1–4; Stories 5–6 are not App Store blockers.

## Release Gates

### Blocking Before Code Freeze

- [ ] Complete or explicitly defer WP-61a Stories 5–6; record the choice in the work packet.
- [ ] Confirm Supabase Auth redirect allowlist contains `https://www.runsmart-ai.com/auth/callback*`.
- [x] Confirm live `https://www.runsmart-ai.com/.well-known/apple-app-site-association` returns HTTP 200 and names `8VC4R5M425.com.runsmart.lite` for `/auth/callback`, `/auth/callback/*`, and `/auth/update-password` (verified 2026-08-13).
- [x] Confirm the paired web callback/AASA repair is merged (`Running-coach-` PR #120).
- [ ] Decide how founder App Store sessions will be excluded. Preferred: add the founder user-id allowlist wiring before freeze; minimum fallback: exclude the known founder distinct ID from the saved release insight and do not treat the public founder smoke as external traffic.
- [ ] Run the overdue physical-device route smoke: record → save route with benchmark → reinstall/persistence check → re-run → benchmark comparison. This has already been shipped past multiple times and is a blocker for another release.
- [ ] Fix the locale-dependent calendar test by pinning its expected locale, or record an explicit baseline exception. Preferred: fix it so the full release suite is genuinely green.

### Should Fix Before Archive

- [ ] Resolve the five main-actor isolation warnings so a future Swift language-mode change cannot turn them into release errors.
- [ ] Migrate the deprecated `HKWorkout` construction to `HKWorkoutBuilder`, or explicitly defer it with an owner and follow-up because it is not new in 1.1.6.
- [ ] Re-run the Release build and record the final warning count. The target is zero; any accepted warning must be named in the release handoff.

### Packaging

- [ ] Create the release branch/worktree from fresh `origin/main`, never from the dirty primary auth branch.
- [ ] Confirm App Store Connect has no existing build 31.
- [ ] Set all six synchronized configurations to `MARKETING_VERSION = 1.1.6` and `CURRENT_PROJECT_VERSION = 31`.
- [ ] Replace the 1.1.5 release notes with the approved 1.1.6 copy below.
- [ ] Confirm no unrelated `Localizable.xcstrings` or untracked synchronized Swift files enter the release.
- [ ] Freeze the source SHA and record it in the release handoff before archive.

### Automated Verification

- [ ] Auth callback tests: all five routes/error cases pass.
- [ ] Email sign-in tests: all 15 pass with no test-host abort.
- [ ] WP-61a focused contract tests pass, including first visible frame, pre-auth screens, permission terminals, and cold/warm/background wall reach.
- [ ] Full suite passes with zero unexplained failures/skips.
- [ ] Release simulator build succeeds from the final release commit.
- [ ] Generic iOS Release build or archive succeeds with the ignored `RunSmartSecrets.xcconfig` present at its expected include path.
- [ ] `git diff --check` passes and the release diff contains only approved scope.

### Manual Device QA

Run on a current physical iPhone and the smallest supported simulator/device size.

- [ ] Clean install reaches the signed-out wall after the launch overlay; Apple, email, Terms, and Privacy controls are reachable.
- [ ] Sign in with Apple completes once, ignores a duplicate tap, and reaches onboarding or the existing account.
- [ ] Email create-account and email sign-in both complete against production Supabase.
- [ ] A `www.runsmart-ai.com/auth/callback` Universal Link opens RunSmart; the custom-scheme fallback is also recognized.
- [ ] Terms and Privacy open and dismiss without leaving the wall unusable.
- [ ] HealthKit grant, deny, and dismissal/error paths leave onboarding usable; no prompt loop.
- [ ] Background and foreground the app while signed out; the wall remains usable and does not remount or duplicate visible behavior.
- [ ] Complete onboarding, generate a plan, open the first workout, and return after relaunch.
- [ ] Start, pause, resume, and finish a short outdoor run; confirm Live Activity and post-run summary.
- [ ] Complete the route/benchmark persistence smoke described above.
- [ ] Delete the test account and confirm the same auth method can register/sign in again where applicable.

### Archive Inspection

- [ ] App bundle is `com.runsmart.lite`, version 1.1.6, build 31.
- [ ] Live Activity extension is also version 1.1.6, build 31.
- [ ] `POSTHOG_API_KEY`, Supabase URL, and Supabase publishable key are non-empty in the built app; values are never printed into logs or documentation.
- [ ] Distribution entitlements include Sign in with Apple, HealthKit, `applinks:runsmart-ai.com`, and `applinks:www.runsmart-ai.com`.
- [ ] Distribution signature uses `get-task-allow=false`; dSYMs are present.
- [ ] Privacy manifest, permission descriptions, encryption declaration, icon, display name, and portrait/device-family settings match the shipped behavior.
- [ ] No diagnostics, plans, untracked Swift sources, or secrets are bundled in the app/IPA.

## Proposed App Store Release Notes

RunSmart 1.1.6 improves account access and reliability.

What's new:
- More reliable return to RunSmart from supported email account links
- Improved email sign-in stability
- General reliability and performance improvements

Do not claim that activation improved. This release makes activation observable; effectiveness is a post-release measurement.

## App Store Connect Handoff

- [ ] Upload the inspected 1.1.6 (31) archive and wait for processing.
- [ ] Confirm ASC selected build is exactly 31 and matches the recorded archive/source SHA.
- [ ] Confirm privacy questionnaire, support/privacy URLs, age rating, screenshots, and reviewer notes remain current.
- [ ] Reviewer notes should state that Apple sign-in and email/password are both available and that HealthKit access is optional.
- [ ] Submit only after the founder explicitly approves the final archive and metadata.

Because traffic is low and the release has passed a real TestFlight/device auth smoke, prefer a normal 100% release rather than a phased rollout that would further delay the n≥10 measurement gate. Do not add paid traffic merely to manufacture the sample.

## Landing and Effectiveness Plan

### Before Release

Save/freeze this ordered, session-scoped funnel split by `app_build`:

`app_launched → activation_first_frame_rendered → sign_in_wall_reached → sign_in_completed → onboarding_completed → plan_generation_succeeded → first_workout_viewed → run_started → run_completed`

Keep exclusions for internal tester, emulator, sideload, TestFlight, and known App Store Review traffic. Record implementation birthdays separately from production first-seen.

### First Production Event

- [ ] Confirm build 31 emits `app_launched`, `activation_first_frame_rendered`, and `sign_in_wall_reached` in one external session and in that order.
- [ ] Confirm `entry=cold_launch` on first reach and stable `screen=sign_in_wall`.
- [ ] Confirm event and identified-person `is_internal_tester` values behave as designed.
- [ ] Confirm no new permission request is left without a granted/denied/dismissed/failed terminal.
- [ ] Confirm the native callback path can produce `sign_in_completed(method=email)` when exercised.

### T+1 and Sample Gate

Run a T+1 health check only if external build-31 traffic exists; otherwise wait for the first genuine event and report “no cohort,” not zero conversion.

Do not infer a bottleneck or start WP-61b until at least 10 genuine users exist at every launch-to-wall step. At n≥10:

1. Report counts and conversion at each launch-to-wall step, not percentages alone.
2. Separate cold launch, warm foreground, and background-return wall entries.
3. Report pre-auth screen dismissals and permission outcomes by kind.
4. Identify the largest evidenced loss and only then decide whether the account gate, wall copy, auth methods, or another surface deserves a product experiment.

## Rollback / Stop Conditions

- If archive or device auth verification fails, do not upload; keep 1.1.5 live.
- If TestFlight cannot return from the canonical Universal Link, stop and fix the AASA/redirect allowlist before submission.
- If a release-build auth or run-recording crash appears, reject the candidate and cut a corrected build number; never reuse build 31 after upload.
- If production telemetry is absent but the build has genuine launches, treat it as an instrumentation incident before interpreting activation.
- If production auth regresses, halt distribution and restore the last known-good path before running activation experiments.

## Explicitly Out of Scope

- Guest mode or value-before-auth redesign (WP-61b remains blocked).
- New onboarding, route, Garmin, coach, monetization, localization, or paywall features.
- Resumely changes or any mutation to Resumely 1.4.9 while it is under App Store review.
- App Store upload/submission in the planning session.
