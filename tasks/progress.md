**1.1.1 (25) — sign-in wall instrumented, plan-generation double-fire fixed (WP-47 S1, 2026-07-20):** The activation-cliff plan's S1 landed as the first content of 1.1.1. `SignInView` now emits `sign_in_wall_viewed` / `sign_in_wall_tapped` / `sign_in_wall_abandoned`, and every wall event (including `sign_in_completed` / `sign_in_failed`) carries an explicit `screen: "sign_in_wall"` property, because SwiftUI `$screen` autocapture emits the same generic `UIHostingController<...>` string for every screen and cannot identify the wall. `sign_in_failed` already carried `error_domain`/`error_code`; those are now on a named screen, so the open ASAuthorizationError-1000 question (production outage vs. environment noise) answers itself for future users without needing a device repro each time. Semantics are enforced by `SignInWallTracker` and pinned by tests: `viewed` at most once per app session; `abandoned` only on background after >=5s dwell with no sign-in attempt, at most once; a tap permanently disarms abandonment so returning to the foreground and signing in never retro-emits. **Double-fire root cause and fix:** `PlanGenerationStore` was a UI-state observer doubling as the analytics emitter, so *any* terminal state transition produced a funnel event — two terminal notifications back to back (the shape seen on founder device `0efa0d1b`, six `plan_generation_failed`/`succeeded` pairs 19ms apart) each flipped state and each emitted, meaning one of every pair was lying. Terminal events are now matched to an observed start and consume it, so a second terminal post is silent and `duration_ms` is always present; a terminal status with no observed start emits nothing, keeping started→succeeded/failed a closed funnel. Both double-fire tests were confirmed to **fail** against the pre-fix implementation before being accepted. Version bumped **1.1.0 (24) → 1.1.1 (25)** in the project only. **Active story:** none in flight. **Next recommended story:** plan **S0** — the founder-only device test that settles whether Sign in with Apple is actually broken for new users; it is ~20 minutes and it reorders everything below it, including whether S2 (value-before-account) is treating a symptom. **Blockers:** S0 and ASC submission are founder-only and were not performed this session; no physical device or ASC access from this environment. **Last validation:** full iOS suite **313 passed / 0 failed / 0 skipped** on iPhone 17 / iOS 26.5, xcresult-verified (`/private/tmp/rs-wp47.xcresult`), 306 baseline + 7 new tests. **Not done:** no device repro of ASAuthorizationError 1000, no archive, no ASC upload or submission, no edge-function deploy, no S2 work. Last Updated: 2026-07-20.

**1.1.0 (24) APPROVED and LIVE on the App Store (2026-07-20):** Apple approved and released **1.1.0**, replacing 1.0.9 (23) as the public version. Release timestamp **2026-07-19T21:48:08Z**, verified against Apple's public lookup API (`itunes.apple.com/lookup?bundleId=com.runsmart.lite`, US and IL storefronts both return `version: 1.1.0`) — this is store ground truth, not a repo or ASC-portal claim. The live build ships Adaptive Coach Phase 1 **flag-ON for all users** (PR #102, `c6e75c1`) against `coach_message` edge **v10**, which was deployed ahead of release as required. Resumely 1.4.3 went live one minute earlier, so both apps are now live simultaneously for the first time. **Carried forward unchanged from the 2026-07-19 autopsy:** 1.1.0 contains **none** of the sign-in wall fixes, so the activation cliff (22/23 organic users producing zero events after app open) is still live in the shipped build, and the Adaptive Coach Phase 2 gate (>=20 real `adaptive_coach_shown`) still cannot fill while the wall stands. **Active story:** none in flight. **Next recommended story:** plan S1 — instrument the sign-in wall (`sign_in_wall_viewed/tapped/abandoned`, screen names, double-fire fix) as the first content of 1.1.1. **Blockers:** none technical. **Last validation:** App Store live version confirmed 1.1.0 via Apple lookup API on 2026-07-20; 306/306 iOS suite passed on the shipped tree (2026-07-19). **Post-release watch (not yet done):** confirm `adaptive_coach_shown` fires from a clean 1.1.0 install in PostHog, and device-smoke the live AI path — AI response quality on the v10 function remains unverified on device.

**Status snapshot + activation autopsy, 2026-07-19 (superseded on release status only):** **1.1.0 (24) submitted to ASC, Waiting for Review** (public at the time: 1.0.9 (23)); ships Adaptive Coach Phase 1 flag-ON (PR #102) and `coach_message` edge v10. Same day, the cross-product activation autopsy landed (PR #100, `docs/plans/2026-07-19-activation-cliff-fix-plan.md`): founder/QA-excluded PostHog read shows **22/23 organic App Store users (95.7%) produce zero events after app open** — the uninstrumented sign-in wall (`RunSmartLiteAppShell.swift:184`) is the leading hypothesis; 0 organic `run_started` in the entire App Store era; build-21 plan→run question closed as unverifiable (no organic denominators on 1.0.9+). **1.1.0 contains none of the wall fixes**, so the Adaptive Coach Phase 2 gate (>=20 real `adaptive_coach_shown`) cannot fill while the wall stands. **Active story:** none in flight. **Next recommended story:** plan S1 — instrument the wall (`sign_in_wall_viewed/tapped/abandoned`, screen names, double-fire fix) as the first content of 1.1.1; then S2 (value-before-account). **Blockers:** none technical; S2 replay portion gated on privacy guardrails. **Last validation:** 1.1.0 (24) archive + upload (2026-07-19); autopsy is analysis-only, no code claims.

**`coach_message` edge function DEPLOYED v9 → v10; Adaptive Coach live AI path now enabled (2026-07-19):** Founder approved the deploy. The Supabase CLI could not be used: `npx supabase` reported `LegacyPlatformAuthRequiredError` and no token existed at `~/.supabase/access-token`, in the login keychain, or in the environment, so the founder's login never persisted. Deployed instead through the connected **Supabase MCP server**, uploading all three required files (`coach_message/index.ts`, `coach_message/flex_week.ts`, `_shared/cors.ts`) with `verify_jwt: false` preserved from v9. A first attempt failed with `Entrypoint path does not exist` because the entrypoint file itself was omitted from the payload; corrected and redeployed. Result: **version 10, ACTIVE**. Post-deploy boundary smoke: unauthenticated POST → **401** `Missing bearer token` (proves the function booted and the relative `../_shared/cors.ts` import resolved), OPTIONS with no Origin → **200** (the native-iOS path), OPTIONS with a disallowed Origin → **403** (origin gate active). The deploy carried a second, previously-undeployed change dated 2026-07-11: `index.ts` moved from an inline wildcard `Access-Control-Allow-Origin: *` to the shared `_shared/cors.ts` allow-list with `isOriginAllowed` gating, plus a 2000-character chat-message cap. Verified safe before deploying: `isOriginAllowed` returns true when `Origin` is absent (native iOS never sets it), no RunSmart Web or cross-project code references `coach_message`, so no browser client can be locked out by the allow-list naming `runsmart.app` rather than `runsmart-ai.com`. **Parity gap found and logged (verified, pre-existing, not a regression):** the edge-side `flex_week.ts` `downgradedEasy` still lacks the PR #101 fix that the Swift builder received, so a server-side fallback can still emit `Easy Run · 8 x 400m`; with the flag now ON this path is user-reachable for the first time. See `tasks/todo.md` for the fix definition. Live AI response quality remains unverified on device. Last Updated: 2026-07-19.

**RunSmart 1.1.0 (24) — Adaptive Coach ON — built, archived, and UPLOADED to ASC (2026-07-19):** Founder reversed the same-day flag-OFF decision and chose **flag ON for all users**. Release branch `release/1.1.0-build24-adaptive-coach-on`: `RUNSMART_ADAPTIVE_COACH_ENABLED` NO→YES in `RunSmartInfo.plist`, `MARKETING_VERSION` 1.0.9→1.1.0, `CURRENT_PROJECT_VERSION` 23→24. Full iOS suite re-run on the flag-ON tree: **306 passed / 0 failed / 0 skipped**, verified from the xcresult bundle (not pipe output). PR **#102** merged to `main` as **c6e75c1** with CodeRabbit + GitGuardian green. Archived `RunSmart-1.1.0-build24-20260719.xcarchive` under `/private/tmp/runsmart-archives/` (ARCHIVE SUCCEEDED; flag YES re-verified inside the archived bundle Info.plist), exported with `ExportOptionsAppStoreUpload.plist` destination=upload: **"Upload succeeded" 12:43 IDT, "Uploaded IOS RunSmart app"** — build 24 now processing in App Store Connect. **Remaining founder steps:** (1) ASC portal: create version 1.1.0, attach build 24, submit for review; (2) `npx supabase login` then deploy `coach_message` (staged command in tasks/todo.md) — MUST land before Apple releases the build because the flag ships ON and the live AI path otherwise runs against the old deployed function; (3) post-deploy device smoke of the live AI path. Phase 2 reminder placed in cross-project auto-memory (`adaptive-coach-phase2-pending`): gate = 2 weeks live AND ≥20 real `adaptive_coach_shown` users, founder/QA-excluded. Last Updated: 2026-07-19.

**Adaptive Coach Phase 1 — operationally complete; only founder gates remain (2026-07-19):** Claude cross-vendor review of the GPT-5.6-authored device-QA fixes (PR **#101**) completed and approved: QA readiness injection is production-isolated (guarded by `-RUNSMART_ADAPTIVE_COACH` and reached only through `DemoRunSmartServices`, production trigger logic untouched); `downgradedEasy` now clears `targetPaceSecondsPerKm`, `workoutStructure`, and sets honest easy-effort detail; the `\d+(?:\.\d+)?\s*[x\u00d7]\s*\d+` repetition regex requires digits on both sides so it catches `8 x 400m` without firing on prose containing a bare letter x, and both directions are pinned by tests. PR #101 squashed to `main` as **e097881**. CodeRabbit and GitGuardian both passed. **Deno gate closed:** Deno 2.9.3 installed to `~/.deno`; `deno test --allow-all supabase/functions/coach_message/index_test.ts` returned **9 passed, 0 failed** covering sanitizer camelCase/snake_case load fields, all five fallback restructure paths, and long-run-removal rejection. Founder decision recorded: release flag stays **OFF** for the next build (`RUNSMART_ADAPTIVE_COACH_ENABLED = NO` verified in `RunSmartInfo.plist`; bundled, not remotely flippable). `coach_message` remains **undeployed by decision** — deploy command staged in `tasks/todo.md` against project `dxqglotcyirxzyqaxqln`, pending explicit second approval; live AI path therefore still unverified on device. Version remains 1.0.9 (23); no archive or ASC submission this session. Non-blocking, unverified review note: a distance string carrying intensity wording without a rep pattern (e.g. `8 km tempo`) would survive a downgrade; no such value exists in fixtures or code today. **Phase 2 entry gate flagged as at risk:** the spec requires 2 weeks of live Adaptive Coach funnel data, which cannot start while the flag ships OFF, and at current traffic (82 users/90d, 9 ever generating a plan) the `adaptive_coach_shown` trigger population is likely too small to produce a readable funnel. Last Updated: 2026-07-19.

**Adaptive Coach Phase 1 — physical-device fallback QA passed; two device findings fixed (2026-07-19):** On a connected physical iPhone, the initial `-RUNSMART_ADAPTIVE_COACH -RUNSMART_DEMO_MODE` launch showed no card because the demo fixture only contained a missed workout on some weekdays and otherwise supplied healthy readiness 82. Fixed the QA-only demo service to provide deterministic readiness 38 when the Adaptive Coach QA flag is active; production policy is unchanged. Device Review then exposed a contradictory safe-fallback proposal, `Intervals · 8 x 400m → Easy Run · 8 x 400m`, because `downgradedEasy` preserved interval prescription fields. Fixed downgrades to clear target pace/workout structure, set honest easy-effort detail, and replace repetition-based distance labels with the conservative `5.0 km` fallback. Device recheck passed: card visible (`Recovery is running low`, readiness 38); Review produced one coherent change (`Intervals · 8 x 400m → Easy Run · 5.0 km`) with rationale, safety note, Confirm and Keep Original actions; confirmation completed and closed normally; dismiss hid the card and remained persisted after a clean process relaunch while Today loaded normally. An intermediate black screen was reproduced as a device-locked relaunch artifact, not an app crash. Full fixed-tree suite **306/306** passed; focused Adaptive Coach + Flex Week suites **44/44** passed. Safe deterministic fallback is device-QA'd. Live AI remains unverified until `coach_message` deploy approval, Deno tests, and live-path smoke. Branch `codex/adaptive-coach-device-qa-fixture` awaits required Claude review before merge. Last Updated: 2026-07-19.

**Adaptive Coach Phase 1 — merged; founder release gates active (2026-07-19):** Prerequisite `fix/flexweek-duplicate-slot-ids` merged to `main` first, then `feat/adaptive-coach-phase1` rebased cleanly except for a task-memory prepend conflict that preserved both entries. Full reviewed-tree iOS suite passed **303/303** on iPhone 17 Pro / iOS 26.5; `build-for-testing`, `git diff --check`, and both plist lint checks passed. Deno is not installed, so the written sanitizer tests were not run. GPT-5.6 Sol cross-vendor review of the Claude Opus 4.8-authored diff found no blocking issue; CodeRabbit and GitGuardian passed. PR **#99** merged to `main` as `8eef381`. The bundled flag remains `NO` and is not remotely flippable. `coach_message` remains undeployed. Archive/upload is blocked on explicit founder decisions for edge deployment and flag ON/OFF, plus device QA or an explicit waiver for Review → diff → confirm and dismiss persistence. Recommendation: ship OFF unless device QA passes. Non-blocking review debt: two Swift actor-isolation warnings from `TrainingLoadCalculator` method references under the project default isolation setting. Last Updated: 2026-07-19.

**Adaptive Coach Phase 1 — Tasks 4-6 complete, feature-complete behind flag (2026-07-18):** **Task 4** production `AdaptiveCoachCard` on Today: wired into the first card slot of `TodayTabView`; prompt computed by `AdaptiveCoachPolicy` from weekWorkouts + readiness (recommendation, falling back to recovery) + `TrainingLoadCalculator` over already-loaded recentRuns; dismissal persists via `@AppStorage` (24h policy cooldown); analytics `adaptive_coach_shown` (deduped per prompt) + `adaptive_coach_action` {review|dismiss} x trigger following the flex_week_intervention pattern; Review opens Flex Week preselected (missedWorkoutReschedule/todayLink). Sim evidence: card live via `-RUNSMART_ADAPTIVE_COACH -RUNSMART_DEMO_MODE` (`docs/qa/reports/assets-2026-07-18-adaptive-phase1/adaptive-coach-card-today.png`); tap-through = founder smoke (no scriptable tap). **Task 5** load-aware AI request: `ReadinessContext` +acwr/acuteLoad/chronicLoad/loadStatus (attached only when data sufficient), camelCase DTO passthrough, `FlexWeekEntryView` fetches recentRuns; edge `flex_week.ts` sanitizer keeps both spellings + system-prompt ACWR rule; FlexWeekTests 30/30. Deno tests written, NOT run locally (no deno); edge function NOT deployed (needs explicit founder approval). **Task 6** telemetry folded into Task 4; funnel = adaptive_coach_shown → adaptive_coach_action(review) → existing flex_week_confirmed. Flag `RUNSMART_ADAPTIVE_COACH_ENABLED` still ships **NO** — rollout gate: founder/QA PostHog exclusions confirmed, then 2 weeks of funnel data before Phase 2. Status: Phase 1 code complete on `feat/adaptive-coach-phase1`, PR pending full-suite gate. Blockers: none. Last Updated: 2026-07-18.

**Adaptive Coach Phase 1 — Tasks 1-3 of 6 (2026-07-18):** Founder greenlit productionizing a recovery- and load-aware proactive coach (Agentic OS DECISIONS.md 2026-07-18; plan of record `docs/specs/2026-07-18-adaptive-coach-phase1.md`). Landed on `feat/adaptive-coach-phase1`, all TDD with witnessed RED: **Task 1** `TrainingLoadCalculator` — Foster session-RPE load + 7d/28d ACWR, bands insufficientData/detraining/optimal/elevated/highRisk; result type `TrainingLoadMetrics` (renamed: `TrainingLoadSnapshot` was taken by the Activity-tab display model), 7/7 tests. **Task 2** `RunSmartFeatureFlags.adaptiveCoachEnabled` — Info.plist `RUNSMART_ADAPTIVE_COACH_ENABLED` ships `NO`, `-RUNSMART_ADAPTIVE_COACH` QA launch arg, 4/4 tests. **Task 3** `AdaptiveCoachPolicy` — trigger priority missedWorkout > loadSpike (highRisk ACWR) > lowRecovery (readiness < 45, stricter than the passive link's < 60), 24h dismissal cooldown; `AdaptiveCoachPresentation.shouldShow` = flag AND prompt; 9/9 tests. All runs are real XCTest passes on iPhone 17 sim (iOS 26.5) with derived data under /private/tmp (see 2026-07-18 lesson). **Next:** Task 4 production `AdaptiveCoachCard` + TodayTabView wiring (founder review checkpoint), Task 5 load-aware Flex Week AI request (client DTO + edge function; deploy needs explicit approval), Task 6 telemetry funnel. Status: Phase 1 in progress. Active story: Task 4. Blockers: none. Last validation: 20/20 new-unit tests green, 2026-07-18.

**FlexWeek duplicate-slot-ID fix (P1 bugfix), 2026-07-18:** Cherry-picked the one production-relevant piece of the adaptive preview branch (c920bbb): `DeterministicFlexWeekBuilder` rescheduled a missed workout (and relocated displaced workouts on the traveling path) keeping the original UUID while the vacated slot's rest-day replacement kept the same UUID — one restructured week could carry two workouts with one identity, breaking SwiftUI ForEach diffing in `FlexWeekDiffView`. Fix: moved workouts adopt the destination slot's UUID via `reidentified()`. **Validation (TDD):** two new regression tests (`testMissedWorkoutRestructureKeepsWorkoutIDsUnique`, `testTravelingRestructureKeepsWorkoutIDsUnique`) confirmed RED on the unfixed builder, then full `FlexWeekTests` **29/29 passed** on iPhone 17 sim (iOS 26.5) — a real XCTest run, not build-for-testing. Unblocked the long-standing simulator-runner stall: CodeSign "detritus" failures were iCloud file-provider xattrs on in-repo derived data; building with `-derivedDataPath` under `/private/tmp` fixes both signing and the "waiting for workers" stall (documented in `tasks/lessons.md`). Branch `fix/flexweek-duplicate-slot-ids`.

**WP-43 S1 — Plan-generation waiting/failure state (P0, Critical), 2026-07-14:** Closed audit §4 Risk 1. After onboarding, Today/Plan sat blank for ~30-45s while generation ran, and the only failure signal was a transient banner pointing at a Profile-buried "Training Data" screen. New `Services/PlanGenerationStore.swift`: `PlanGenerationState` (.idle/.generating/.ready/.failed) + `showsGeneratingCard`/`showsInlineRetry`; store observes the existing `runSmartPlanGenerationStatusDidChange` (`queue: nil` → synchronous main-thread delivery since every post is inside `MainActor.run`; also makes tests deterministic) and emits WP-45 `plan_generation_started/succeeded/failed` with `duration_ms`. New `Features/Plan/PlanGenerationStatusCard.swift`: "Coach is building your plan" + spinner + "30 to 45 seconds"; on failure "We couldn't build your plan" + inline **Try again**. Wired via `@StateObject`/`.environmentObject` in the shell and `@EnvironmentObject` in Today+Plan, rendered after the header; retry calls `saveTrainingGoal(TrainingGoalRequest.onboardingDefault(from: session.onboardingProfile))` without leaving the screen (`onboardingDefault` extracted from the shell so onboarding + retry build identical requests). `RunSmartPlanNotice.failed` copy no longer references "Training Data". **Validation:** red confirmed exactly per plan (mapped `.generating`→`.idle` → **TEST FAILED**: `("idle") is not equal to ("generating")` / "must show the generating card, never a blank body"), green after restore; `testPlanGenerationStateTransitionsGeneratingToReady`, `testPlanGenerationFailureExposesInlineRetry`, `testPlanGenerationFailedNoticeDoesNotPointAtBuriedScreen` all pass; **178 tests, 0 failures**. Debug build clean; iPhone 17 QA confirms Today + Plan still render with the new EnvironmentObject (no crash) — `docs/qa/reports/assets-2026-07-14-wp43-s1/`. **Bonus (completes S5's acceptance):** Plan QA exposed "Imported activity · Heuristic" still live from a *third* enum `PlanExplanationSource.displayName` (TodayTabView:511) — the audit's actual §10 B12 evidence string, missed by S5's plan file list. Mapped to user copy + `testPlanExplanationSourceDisplayNamesAreUserFacing`; screenshot now reads "Imported activity · Based on your plan". **Follow-ups:** generating/failed cards not yet seen on a real device (need a live generation cycle) — founder smoke recommended; `plan_generation_timed_out` defined but not wired to the 45s poll. Report: `docs/qa/reports/wp43-s1-plan-generation-state.md`. Branch `claude/bold-noyce-678ace`.

**WP-43 S6 — Require a visible Goal selection + fix hidden default (P0), 2026-07-14:** Closed audit §4 Risk 9 / §10 B15. `OnboardingProfile.empty.goal` was `"10K improvement"` — not one of the five visible options — so a user who never picked a goal got a plan silently built around it. Fix: (1) `RunSmartModels.swift` default goal → `""`; (2) `OnboardingView.swift` — `static goalOptions`/`experienceOptions` + pure predicates `canAdvanceFromGoal/canAdvanceFromExperience`, Goal/Experience Continue now `isEnabled:`-gated (`OnboardingPrimaryButton` gained `isEnabled` defaulting true, `.disabled` + 0.45 opacity), and `.gesture(DragGesture())` on the TabView swallows horizontal swipes so steps advance only via Continue. `experience` keeps "Building base" (already a visible option — the plan's allowed "preselect a visible option" path). **Validation:** `testOnboardingRequiresVisibleGoalSelection` + `testOnboardingDefaultGoalIsNotHiddenValue` — assertion-level red exactly per plan (restored "10K improvement" → **TEST FAILED**: `("10K improvement") is equal to ("10K improvement")`), green after fix; **174 tests, 0 failures** across `RunSmartReadinessTests` + `StriverPersonaGateTests` (both build on `.empty`, no regression). Debug build clean. iPhone 17 QA: Goal step renders, 5 visible options, Continue enabled with a valid selection (`docs/qa/reports/assets-2026-07-14-wp43-s6/onboarding-goal-step.png`). **Known QA gap:** the replay flag prefills goal "First 5K", so the empty-goal disabled state + swipe-block weren't interactively QA'd (no simctl tap/idb/cliclick; computer-use declined) — covered by the predicate test + code review; founder device smoke of onboarding recommended before release. Report: `docs/qa/reports/wp43-s6-onboarding-goal-selection.md`. Branch `claude/bold-noyce-678ace`.

**WP-43 S5 — Remove developer-vocabulary badges (P0), 2026-07-14:** Closed audit §4 Risk 10 / §10 B12. Three sites: (1) `PostRunLearningCard.swift` — added `PostRunLearningSource.displayLabel` (`.heuristic`→"Based on your plan", `.fallback`→"Quick take", `.ai`→"Coach analysis", `.report`→"Coach report"); the COACH LEARNED chip renders `displayLabel` not `rawValue`; raw enum kept for logic/analytics. (2) `ActivityTabView.swift` — deleted the "Source: Real" pill. (3) `TodayTabView.swift` — "Coach safety · Heuristic" → "Coach safety check". Left the separate post-run debrief "AI"/"Coach" chip (`PostRunDebriefModel.Source`) untouched — out of the plan's listed S5 scope (reverted after a compile error surfaced it's a different enum); flagged as possible follow-up. **Validation:** `testPostRunSourceDisplayLabelsAreUserFacing` — assertion-level red confirmed (`displayLabel` returning rawValue → **TEST FAILED**: `("Heuristic") is equal to ("Heuristic")… must not render its raw enum value`), green after mapping; full `RunSmartReadinessTests` **169 tests, 0 failures**. Debug build clean. Demo-mode QA: Report tab on **both iPhone 17 + iPhone SE** shows only RUNS + MOVING TIME (Source:Real pill gone) — `docs/qa/reports/assets-2026-07-14-wp43-s5/report-tab.png`, `report-tab-se.png`. Report: `docs/qa/reports/wp43-s5-dev-vocab-badges.md`. Branch `claude/bold-noyce-678ace`.

**WP-43 S3 — Fix aha-timeline contradiction + remove overpromise (P0), 2026-07-14:** Closed audit §4 Risk 6 / §10 B2. `GoalTimelineMomentView.headline` hardcoded "Six weeks from now…" for any 5K goal while the timeline graphic rendered "in \(timeline.weeks) weeks" — an 8-week persona saw six vs eight. Fix in `GoalTimelineMomentView.swift`: extracted `static func headlineText(for:)` single-sourcing the duration from `timeline.weeks` (5K branch now "In \(timeline.weeks) weeks, you could be lining up at your first 5K."), and `static let sublineText` replacing the guarantee "we know you'll finish" with "Runners who stick with this plan usually get there." **Validation:** `testGoalTimelineHeadlineMatchesMilestoneWeeks` + `testGoalTimelineSublineHasNoGuaranteeLanguage` — assertion-level red confirmed by temporarily restoring the hardcoded strings (**TEST FAILED**: "headline must state the timeline's own 8-week duration… got: Six weeks…"), green after restore; full `RunSmartReadinessTests` **168 tests, 0 failures**. Debug build compiled clean. Aha-overlay screenshot not scriptable (fires post-onboarding; no simctl tap/idb/cliclick, computer-use declined) — output is a pure function of `timeline`, covered by the test across all goal categories × weeks {6,8,12}. Report: `docs/qa/reports/wp43-s3-aha-timeline-contradiction.md`. Branch `claude/bold-noyce-678ace`.

**WP-43 S4 — Fix the Workout Breakdown fabrication "21000 × 400 m" (P0), 2026-07-14:** Closed audit §4 Risk 5 / §10 B3/B4. Root cause: `StructuredWorkoutFactory.intervalSteps` derived reps from `distanceKm(from: workout.distance)`, which digit-strips "8 x 400m" → "8400" → 8400 km → `reps = max(4, Int(8400/0.4)) = 21000`. Fix in `StructuredWorkoutFactory.swift`: added `parseIntervalReps(from:)` (NSRegularExpression) that parses "8 x 400m"/"8 × 400 m"/"10x200m" into `(reps, repDistance)` and returns nil for plain total distances so no rep count is ever synthesized from a digit-stripped total; `intervalSteps` builds the Repeats label from the parse, and labels the hardcoded fallback pace "(est.)" so the sheet stops asserting a precise pace the card never showed. **Validation:** focused XCTest `testIntervalBreakdownParsesRepsFromStructure` + `testBreakdownNeverExceedsPlausibleReps` — red confirmed by stashing only the factory file (old code → **TEST FAILED**, 21000), green after restore; full `RunSmartReadinessTests` **166 tests, 0 failures** (`-parallel-testing-enabled NO`; parallel run's failure was a CoreSimulator cloneDevice infra abort). Debug build compiled clean. Plan tab renders the honest `Intervals · 8 x 400m` (`docs/qa/reports/assets-2026-07-14-wp43-s4/plan-tab.png`); detail-sheet screenshot not scriptable this session (no simctl tap / idb / cliclick; computer-use Simulator control declined) — sheet output proven by the red→green unit test on the exact `makeSteps→intervalSteps` path. Report: `docs/qa/reports/wp43-s4-workout-breakdown-fabrication.md`. Branch `claude/bold-noyce-678ace`.

**WP-43 S2 — Humanize Sign in with Apple failure copy (P0), 2026-07-14:** Closed audit §4 Risk 3 / §10 B1 — a first-time user hitting a SIWA failure saw the raw `com.apple.AuthenticationServices.AuthorizationError error 1000`. Fix in `SignInView.swift`: replaced `errorMessage = error.localizedDescription` with a pure static `humanReadableAppleSignInError(for:)` mapper — `.canceled` → nil (silent), everything else → `"Apple sign-in didn't finish. Nothing was created — tap to try again."` Raw `NSError.localizedDescription` never reaches the UI. Added WP-45 `trackSignInFailed(error:)` → `sign_in_failed { error_domain, error_code }` in `AnalyticsEvents.swift`, called from the catch branch (raw domain/code to analytics only). **Validation:** focused XCTest `testSignInErrorMappingHidesRawNSError` — red state confirmed (compile-failed against the old path, `no member 'humanReadableAppleSignInError'` ×3); green **TEST SUCCEEDED** (asserts no `com.apple`/`1000` leaks, `.canceled` → nil, generic fallback for other errors). Debug build **SUCCEEDED** on iPhone 17. Sign-in screen renders cleanly on iPhone 17 + iPhone SE (`docs/qa/reports/assets-2026-07-14-wp43-s2/`); failure path is unit-tested since a real SIWA server error can't be forced on a simulator. Report: `docs/qa/reports/wp43-s2-siwa-error-copy.md`. Branch `claude/bold-noyce-678ace`.

**WP-40 S1 — Apple Health connect in primary onboarding flow (P0), 2026-07-10:** Added a skippable Apple Health step before onboarding completion, replacing the prior informational-only HealthKit preview. The step reuses the existing `services.connect(provider: HealthKitSyncService.providerName)` route, emits the existing disclosure/connect analytics, refreshes stored connection state, and only advances automatically for a matching connected HealthKit status. Added stable accessibility identifiers for Connect/Skip and a focused red/green XCTest contract. **Validation:** focused XCTest passed (1/1); Debug simulator build succeeded on iPhone 17; onboarding replay exposed both actions; Skip reached Ready; Connect followed the real HealthKit route (current simulator was already authorized, with preserved fresh-permission evidence at `docs/qa/reports/wp40-18-after-wait.png`). `git diff --check` passed. Branch remains uncommitted with pre-existing partial S2 service changes and QA artifacts; S2–S4 are not claimed complete.

**WP-38 S14a–d — Stretch bundle: screen awake, Live Activity, haptics, accessibility (P3), 2026-07-09:** Closed WP-38 packet stretch items as four independent diffs. **S14a:** `RunScreenAwakePolicy` disables idle timer during `.recording`/`.paused`, restores on finish/discard/tab exit. **S14b:** Added `RunSmartRunLiveActivityExtension` widget target with lock-screen/Dynamic Island Live Activity showing distance/moving time/pace plus Pause/Resume + Finish intents (`PauseRunLiveActivityIntent`, `FinishRunLiveActivityIntent`) wired via notifications to `RunTabView`; `RunLiveActivityController` syncs from recorder metrics; shared `RunRecordingLiveActivityAttributes` in `RunSmartShared`. **S14c:** Haptic pass — start medium, pause/resume light, finish light on tap + medium on confirm, discard light on dialog open + medium on confirm (no duplicate fire on single action). **S14d:** VoiceOver labels/hints on `PreRunView`, `LiveRunView` (metrics, controls, KM splits), `PostRunSummaryView`, `RPESelector`; largest Dynamic Type smoke on live HUD without layout clip (existing ScrollView + scale factors). **CodeRabbit fixes (PR #83):** pause a11y hint corrected; `RunTabView.onAppear` restores awake/Live Activity after tab return; DEBUG logging for Activity.request failures; Dynamic Island respects paused state; removed redundant `INFOPLIST_KEY_NSSupportsLiveActivities`. **Validation:** no stray `* 2.*` under app source; Debug build **SUCCEEDED** with embedded extension; sim evidence `docs/qa/reports/assets-2026-07-09-wp38-s14/`. **Merged to main as `f9d7c89` via PR #83 (2026-07-09).**

**WP-38 S12 — Live per-km splits during recording (P2), 2026-07-09:** Surfaced real GPS-derived kilometer splits in the live run HUD as each km boundary completes. Added `LiveKmSplitsPanel` to `LiveRunView` — collapsed-by-default compact row below the map (latest split pace + chevron); expands to list all completed splits. Uses `RunRecorder.kilometerSplits(from: recorder.routePoints)` only (same helper as post-run `PostRunSummaryView.splitRows`); full `routePoints` passed separately from map `displayRoutePoints`. No new pace math. DEBUG-only `-AUTO_START_RUN` (demo mode) for simulator QA automation. **Validation:** no stray `* 2.*` duplicates under app source; Debug build **SUCCEEDED**; WP-37 S5 split unit tests **PASSED**; iPhone 17 sim + `simctl location` 3.5 km path: at 1.21 km live HUD shows **KM SPLITS `0:47 km 1`**; at 2.22 km shows **`0:41 km 2`**; post-run save 2.55 km uses same `kilometerSplits` path. Evidence: `docs/qa/reports/assets-2026-07-09-wp38-s12/`. Branch `claude/wp38-runsmart-s12-live-km-splits`.

**WP-38 S11 — Delete-dialog Garmin copy cleanup (P3), 2026-07-09:** Post-run and Report delete confirmations still mentioned Garmin even for phone-recorded runs (`"It will not delete anything from Garmin."` / `"Garmin runs are hidden..."`). Copy-only fix: both surfaces now use identical source-neutral copy — title `Delete this run?`, message `This removes the run from your RunSmart history.`, destructive `Delete Run`. Also switched both from `.confirmationDialog` to `.alert` so Cancel stays visible on iOS 26 (same WP-37 S4 lesson). Hide-vs-delete behavior unchanged. Old Garmin strings saved in `tasks/garmin-deferred-copy-restore.md` for when Garmin reconnects. **Validation:** Debug build **SUCCEEDED**; device QA on iPhone 17 Report trash → alert shows neutral copy with Cancel + Delete Run, no Garmin wording (`docs/qa/reports/assets-2026-07-09-wp38-s11/`). Batched with S9+S10 for one PR.

**WP-38 S10 — Fix `timeLabel` for runs ≥1 hour (P3), 2026-07-09:** `RunRecorder.timeLabel` always printed zero-padded `MM:SS`, so a 90-minute run rendered as `90:00` instead of `1:30:00`. Fix: branch at `>= 3600` to `H:MM:SS` (`%d:%02d:%02d`); keep existing `MM:SS` under an hour. All live HUD / post-run / history / report-skeleton call sites inherit via the shared helper. Also pointed `PostRunSummaryScaffold`'s local formatter at `RunRecorder.timeLabel`, and fixed demo-mode `runReport(for:)` so unmatched local/seeded runs use `reportSkeleton` (real `timeLabel`) instead of falling back to an unrelated Tempo Builder fixture (`41:36`) — that fallback was masking S10 device QA. Added DEBUG `-OPEN_SECONDARY addActivity` for Add Activity deep-link. Unit tests added: `testTimeLabelUsesHourFormatAtAndAboveOneHour`, `testTimeLabelKeepsMinuteFormatUnderOneHour`. **Validation:** Debug build **SUCCEEDED** on iPhone 17; `build-for-testing` **SUCCEEDED** (dedicated DerivedData); simulator XCTest launch stalled (IDERunDestination empty / install hang — known infra, not a test failure); device QA seeded 5400s + 2700s runs into sim prefs: report detail shows **MOVING TIME `1:30:00`** (90 min) and **`45:00`** (45 min). Evidence: `docs/qa/reports/assets-2026-07-09-wp38-s10/`. Still batched with S9 for one PR after S11.

**WP-38 S9 — Rename "Time" to "Moving time" (P3), 2026-07-09:** Copy-only relabel of every user-facing `"Time"` title whose value is moving-time (`movingLabel` / `movingTimeSeconds` / report duration from that field). Touched live HUD (`RunTabView` metric tile), post-run summary + share fallback (`PostRunSummaryView`), Report totals pill (`ActivityTabView`), run-report / Garmin / scaffold badges (`SecondaryFlowView`), save-route sheet, share card, and the matching `currentRunMetrics` tiles in production/Supabase/demo/live mappers. PreRun already said `"Moving time"`; left banner copy `"Time is running now."` and code identifiers (`timeLabel`, `movingTimeSeconds`) alone. No elapsed-time tracking added. **Validation:** no source `* 2.*` duplicates under app/tasks/docs/supabase; baseline + post-change Debug builds on iPhone 17 sim **SUCCEEDED**. Device-QA with `-RUNSMART_DEMO_MODE` + `simctl location` City Run: live HUD shows **MOVING TIME** (iPhone 17 + SE); post-run summary shows **MOVING TIME**; Report Last-14-Days pill shows **MOVING TIME**. Evidence under `docs/qa/reports/assets-2026-07-09-wp38-s9/`. Branch `claude/wp38-runsmart-s9-moving-time-label`. No unit test (copy-only). PR not opened yet — awaiting founder preference (S9 alone vs batch S9–S11).

**WP-37 S8 — PreRun honesty: real preview, visible Last Run (P2), 2026-07-08:** Fixed the PreRun screen making a decorative route sketch look like live GPS data while the Last Run card could sit below the fold on short screens. Root cause confirmed exactly as scoped: `PreRunView` used a non-scrolling `VStack` inside `GeometryReader`, and the decorative `RunSmartRoutePreview` was labeled `GPS preview` with the GPS icon even though it was not connected to current location. Fix: wrapped the PreRun content in `ScrollView(showsIndicators: false)`, kept the existing tall-screen feel with `minHeight: proxy.size.height`, and relabeled the decorative preview to `Route sketch` with the map icon (`showGPS: false`). No new map/location feature was added. **Validation:** clean worktree `/tmp/rs-wp37-s8` (branched off fresh `origin/main` at `79bc39f`); Debug build on iPhone 17 sim **SUCCEEDED** (known HealthKit `HKWorkout` initializer deprecation warning and run-script dependency note only). No unit test added because this is pure SwiftUI layout/copy honesty. Device-QA on **both** iPhone 17 and iPhone SE simulators using `-RUNSMART_DEMO_MODE -INITIAL_TAB Run`: iPhone 17 shows `Route sketch` and `Last Run` visible/reachable; iPhone SE shows `Route sketch`, and after one scroll the `Last Run` card is reachable and visible. Evidence screenshots saved under `docs/qa/reports/assets-2026-07-08-wp37-s8/`.

**WP-37 S7 — Persist RPE or stop pretending (P2), 2026-07-08:** Fixed the post-run "How did that feel?" selector pretending every run was pre-rated 6/10 while discarding the selected value. Root cause confirmed exactly as scoped: `PostRunSummaryView` initialized `rpe = 6`, `RPESelector` required a non-optional value, and no save path wrote that rating back to the recorded run. Fix: made the selector optional/unset by default (`Not rated`), added `RecordedRun.rpe`, persisted RPE through `RunSmartLocalStore.updateRunRPE`, wired the RunLogging service path for production/Supabase/demo services, and surfaces saved RPE on activity history rows plus run report detail. The post-run coach card now uses neutral "Not rated" copy until the runner makes an explicit selection. The demo service also surfaces locally saved simulator runs ahead of preview fixtures so compressed `-RUNSMART_DEMO_MODE` QA can verify persistence in Report without weakening production visibility rules. **Validation:** clean worktree `/tmp/rs-wp37-s7` (branched off fresh `origin/main` at `cfeb854`); focused XCTest `RunSmartReadinessTests/testLocalStorePersistsRunRPESelection` **PASSED**; red-state check confirmed the test **FAILS** when `saveRun(updated)` is removed from `RunSmartLocalStore.updateRunRPE`, then **PASSES** again after restoring the fix. Debug build on iPhone 17 sim **SUCCEEDED**. Device-QA on **both** iPhone 17 and iPhone SE simulators using `-RUNSMART_DEMO_MODE` and real `RunRecorder`/CoreLocation: saved a run, confirmed the summary starts `Not rated`, selected RPE 8 and saw `8/10`, relaunched into Report and confirmed the saved history row shows `RPE 8/10`. Evidence screenshots saved under `docs/qa/reports/assets-2026-07-08-wp37-s7/`.

**WP-37 S6 — Live map current position is not Finish (P2), 2026-07-08:** Fixed `RouteMapView` marking the live runner position as a red "Finish" flag mid-run. Root cause confirmed exactly as scoped: the shared map component always rendered `Marker("Finish", systemImage: "flag.fill", coordinate: last)` for the final route point, and `LiveRunView` used that same default behavior while recording. Fix: added an `isLive` flag to `RouteMapView` that defaults to `false`; live maps render the first point as Start and the latest point as an unlabeled current-position dot, while all existing post-run/route-library call sites keep Start/Finish markers unchanged. `LiveRunView` is the only caller passing `isLive: true`. **Validation:** clean worktree `/tmp/rs-wp37-s6` (branched off fresh `origin/main` at `825e8d9`); Debug build on iPhone 17 sim **SUCCEEDED** (known HealthKit `HKWorkout` initializer deprecation warning only). No unit test added because this is pure SwiftUI Map annotation presentation with no existing testable logic boundary; visual device-QA covered the acceptance. Device-QA on **both** iPhone 17 and iPhone SE simulators using `-RUNSMART_DEMO_MODE` and real `RunRecorder`/CoreLocation: started simulated GPS recording and confirmed the live map shows Start + an unlabeled current-position dot with no "Finish" marker; finished/saved on iPhone 17 and confirmed the completed route summary still shows Start + Finish flags. Evidence screenshots saved under `docs/qa/reports/assets-2026-07-08-wp37-s6/`.

**WP-37 S4 + SE-clipping follow-up — Dialogs must show a visible cancel button; control row must fit iPhone SE (P1), 2026-07-08:** Fixed two device-confirmed bugs scoped together per founder instruction. (1) `RunTabView`'s Finish and Discard `confirmationDialog`s rendered with only the destructive/primary button on this iOS 26 environment — "Keep Workout"/"Keep Recording" never appeared, leaving a mid-run mis-tap with no visible way back (confirmed via zoomed device screenshot, not simulator-only speculation). Fix: switched both dialogs from `.confirmationDialog` to `.alert`, preserving identical copy, button labels, and roles (`Button("Discard Workout", role: .destructive)` / `Button("Keep Workout", role: .cancel)`; `Button("Finish and Save")` / `Button("Keep Recording", role: .cancel)`). (2) The same smoke pass found `LiveRunView`'s bottom control-row labels ("Pause"/"Finish"/"Coach", "Resume"/"Finish"/"Discard") clipped off-screen on iPhone SE (375×667pt) — a non-scrolling `GeometryReader`+`VStack` with fixed-height-floor panels (`max(174, min(218, proxy.size.height * 0.25))`) left less room than the row needed, with no scroll fallback. Fix: wrapped the body `VStack` in `ScrollView(showsIndicators: false)` and changed its frame modifier from `maxHeight: .infinity` to `minHeight: proxy.size.height`, preserving the existing bottom-pinned button layout (via `Spacer(minLength: 0)`) on tall screens while making short screens scroll instead of clip. **Validation:** clean worktree `/tmp/rs-wp37-s4` (branched off post-S1/S2/S3/S5 `main` at `77441fd`); full `RunSmartReadinessTests` suite build+test on iPhone 17 sim **PASSED** (no new tests — both fixes are pure SwiftUI presentation/layout, unchanged by any existing test target). Live device-QA on **both** iPhone 17 and iPhone SE simulators: started a run, confirmed the Finish alert shows both "Finish and Save" and "Keep Recording", tapped "Keep Recording" and confirmed the run kept recording without state loss; paused, confirmed the Discard alert shows both "Discard Workout" and "Keep Workout". On iPhone SE specifically: confirmed "Pause"/"Finish"/"Coach" and "Resume"/"Finish"/"Discard" labels are now fully visible after a scroll gesture (previously clipped past the screen edge with no way to reveal them), and confirmed both dialogs render both buttons on SE as well as iPhone 17.

**WP-37 S3 — Paused control row must fit the screen (P1), 2026-07-08:** Fixed `LiveRunView`'s paused control row overflowing every supported width. Root cause confirmed exactly as scoped: Resume(112) + Finish(78) + Coach(78) + Discard(78) + 3×18 spacing + 36 padding = 436pt, wider than iPhone 17's 402pt (worse on smaller phones). Fix (smallest diff, exactly per WP-37's proposed change): the row never shows 4 buttons — while `.paused`, Discard replaces Coach instead of appending a fourth button (Coach/mute is inert while paused since `VoiceCoachService` is itself paused, so there's nothing to mute). New max width: 112+78+78+2×18+36 = 340pt on every phase. **Validation:** clean worktree `/tmp/rs-wp37-s3` (branched off post-S1/S2 `main`), Debug build on iPhone 17 sim **SUCCEEDED**. No existing unit/UI tests target `LiveRunView` (pure layout, no store); the diff only swaps which button renders in the paused branch — `onPauseResume`/`onFinish`/`onDiscard`/voice-coach action closures are untouched. **Merged to main as `45aec7b` via PR #75 (2026-07-08).**

**WP-37 S5 — Remove fabricated KM splits (P1), 2026-07-08:** Fixed `PostRunSummaryView.splitRows` presenting invented paces as real "KM SPLITS". Root cause confirmed exactly as scoped: `let drift = Double((km % 3) - 1) * 4; let pace = max(1, run.averagePaceSecondsPerKm + drift)` — every split was the run's average pace plus a fixed synthetic wobble, not derived from GPS data at all. Fix: added `RunRecorder.kilometerSplits(from: routePoints, maxSplits: 8)`, a pure function that walks route-point GPS distance (`CLLocation.distance(from:)`, same pattern as `RouteMatchingService`) and only emits a split for kilometer N once the recorded route actually crosses N×1000m, with the real elapsed time between crossings as the pace — a run that never completes a full km now correctly returns zero splits instead of one fabricated "filler" split (the old `max(1, Int(distanceMeters/1000))` guaranteed at least 1 split for any run ≥500m, even a 600m run that never ran a full km). New `KilometerSplit` result type added next to `RunRoutePoint` in the models file. Updated the now-inaccurate empty-state copy from "Splits appear after at least 500m" to "...once you complete 1 km" to match the real threshold. `SplitPreviewCard`/`SplitRow` (presentation) untouched. **Validation:** clean worktree `/tmp/rs-wp37-s5` (branched off post-S1/S2/S3 `main`), iPhone 17 sim — full app build **SUCCEEDED**, 2 new focused tests PASS (`testKilometerSplitsComputesRealPaceFromRoutePointCrossings` — exact real pace math over GPS-crossing fixtures, 5:00/km then 5:30/km; `testKilometerSplitsReturnsEmptyWhenRouteNeverCompletesAKilometer`); red-state check confirmed the pace-math test FAILS when a wrong/fabricated value is substituted for the real elapsed-time computation. **Rebased onto post-S3 main (`45aec7b`) 2026-07-08 to resolve conflict with S3's progress.md entry; source files had zero conflicts (disjoint from LiveRunView.swift).**

**WP-37 S1 — Zombie recorder reset (P0), 2026-07-08:** Fixed the frozen "Recording" screen after finish/discard/delete. Root cause: `RunRecorder.finish()`/`discard()` delegated post-run phase reset to `updatePhaseForAuthorization()`, which never resets phase out of `.recording`/`.paused` when authorized (only `.idle/.requestingPermission/.denied/.failed`); the existing discard test passed only because the test host is `.notDetermined`. Fix: added `resolveTerminalPhase()` (always exits the live state → `.ready`/`.denied`/`.idle` by authorization), called from `finish()` and `discard()`; `finish()` now also calls `resetCurrentRun()` to clear stale metrics; kept `updatePhaseForAuthorization()` on the auth-change callback so a mid-run permission change can't yank an active run. Added an injectable `authorizationStatusProvider` seam so the authorized-path P0 is testable. **Validation:** clean worktree `/tmp/rs-wp37-s1` (main's `* 2.swift` dupes excluded), iPhone 17 sim — 3 focused tests PASS (`testRunRecorderFinishReturnsToReadyPhaseWhenAuthorized`, `testRunRecorderPauseThenDiscardReturnsToReadyPhaseWhenAuthorized`, existing `testRunRecorderDiscardResetsCurrentWorkoutWithoutSaving`); red-state check confirmed the finish test FAILS when the bug is reintroduced. **Merged to main as `b97064c` via PR #73 (2026-07-08).**

**WP-37 S2 — Don't abort a run on transient GPS errors (P0), 2026-07-08:** Fixed `RunRecorder.locationManager(_:didFailWithError:)` unconditionally setting `phase = .failed` on any error while recording/paused, which silently kicked an active run back to PreRun and lost it on a transient `kCLErrorLocationUnknown`-class failure. Fix: while `.recording`/`.paused`, only an explicit `CLError.denied` stops the run (`stopTracking()`, `phase = .denied`); every other error keeps recording and surfaces "Weak GPS signal. RunSmart keeps recording and will reconnect automatically." via `lastErrorMessage`; `acceptRecordingLocation` clears `lastErrorMessage` on the next good fix so the pill doesn't stay stale. Non-recording states keep the prior any-error→`.failed` behavior unchanged. **Validation:** clean worktree `/tmp/rs-wp37-s2` (branched fresh off `main`, independent of S1), iPhone 17 sim — 3 new focused tests PASS (`testRunRecorderIgnoresTransientGPSErrorWhileRecording`, `testRunRecorderStopsSafelyWhenPermissionDeniedWhileRecording`, `testRunRecorderStillFailsOnErrorWhenNotRecording`); red-state check confirmed both new tests FAIL when the old unconditional-fail behavior is reintroduced. **Corroborating finding (fixed by S1/PR #73, already merged):** while validating, found the pre-existing `testRunRecorderDiscardResetsCurrentWorkoutWithoutSaving` failing on **unmodified `main`** in this environment — verified in an isolated pristine worktree with zero edits — because this Mac's iPhone 17 simulator has drifted to real `.authorizedWhenInUse` location authorization instead of `.notDetermined`, which is exactly the S1 zombie-phase root cause manifesting live in the existing suite; S1 was already merged by the time S2 rebased onto `main`, so this test now passes again post-rebase. **Rebased onto post-S1 `main` (`b97064c`) 2026-07-08 to resolve merge conflicts with S1's changes to the same file; both fixes coexist and verified together.** **Device QA still owed (both S1 and S2):** record ≥1 min → Save → confirm Run tab shows PreRun with Start + tab bar; repeat via View Report and Delete; pause→discard→PreRun; start a second run, metrics from 0. Switch `simctl location` scenarios mid-run (or toggle location) → run keeps recording, pill shows degraded-GPS copy, no data loss; denying permission mid-run still stops safely. Branch `claude/wp37-runsmart-s2-gps-transient-errors`. Files: `Services/Production/RunSmartProductionServices.swift`, `IOS RunSmart appTests/RunSmartReadinessTests.swift`. Next: S3–S8 not started.

**Zero-streak Profile fix (release blocker for 1.0.9), 2026-07-15:** Post-upgrade review of the FTUX audit board found a brand-new runner saw "0 day streak" on Profile while Today correctly showed nothing — the same two-surface disagreement WP-44 S3 exists to kill, hitting first-time users specifically. Root cause: `canonicalStreakLabel` already returns nil for zero ("nothing to show"), but `ProfileTabView.streakDisplay` was typed `String` and overrode it with `?? runner.streak`, re-rendering the raw backend label (`SupabaseRunSmartServices.swift:507` sends literally "0 day streak" for a user with no streak record). The fallback protected nothing: its only non-day-streak source is `ProductionRunSmartServices`' "3x/week" cadence, reachable only via `LiveRunSmartServices`, which is never instantiated (dead code). Fix: `streakDisplay` → `String?`, Profile hides the label **and its separator dot**; `ProgressSharePayload.milestone` takes `value: String?` and omits the metric so a streakless milestone can't share a dangling "Progress: ". **Validation:** `testNewRunnerSeesNoStreakOnEitherSurface` pins the real backend labels ("0 day streak"/"0 days"/"--") → nil on both surfaces, red state confirmed (dropped the >0 guard → **FAILED**: `"0 day streak" - Profile must render nothing for a new runner`), green after restore; `testMilestoneShareOmitsMissingProgressValue` covers the share edge; full suite **275 tests, 0 failures**; Release-config build SUCCEEDED from the main checkout. Device QA: `docs/qa/reports/assets-2026-07-14-v109-smoke/06-profile-zero-streak.png` (zero streak → "10K focused" alone, no dot) and `04-profile.png` (normal → "11 day streak" with dot). Merged `3186343` via PR #92.

**Audit board refreshed 2026-07-15:** the FTUX journey board artifact now carries per-finding status — **11 of 15 audit findings closed**, 1 reclassified as demo-fixture-only (Profile "10K focused · Peak Performer" — the real Supabase path derives goal/level from the user's profile), 3 deliberately open (auth wall → Experiment E2; "Free Run" default and per-row delete → the audit's own "observe before changing" list). Not re-scored: no second audit has run, so 5.5/10 stays labelled "at audit". Also confirmed demo-only, not production bugs: B9 (NEXT RUNS listing completed past runs — production `nextWorkouts` filters `date >= today && !completed`; the demo service returns raw fixtures). WP-45 newly unblocks audit §12 items #15 (`run_started(source:)`) and #17 (`run_report_generate_tapped`). Still partially addressed: B4 (breakdown fallback pace labelled "(est.)" rather than reconciled with the card chip).

**Release prep 1.0.9 (23) + PR #89 review closeout, 2026-07-14:** PR #89 review ran 4 passes (critical + testing/maintainability/security specialists + adversarial): security clean, 15 findings triaged, 9 fixed + 1 false positive dismissed with evidence (timezone claim — shortDate pins .current), merged as `6c6162b`. Post-merge release prep on `release/1.0.9-build23`: MARKETING_VERSION 1.0.8→1.0.9, CURRENT_PROJECT_VERSION 22→23 (all 6 build-setting sites), release notes rewritten for the FTUX story. **Two live bugs caught by v1.0.9 simulator smoke and fixed:** (1) Plan week total rendered the audit's literal 86.20km — `distanceKm(from:)` counted "8 x 400m" as 8 km and Strength's "45 min" as 45 km; new `plannedDistanceKm(from:)` expands interval notation via the WP-43 S4 parser and counts non-distance labels as 0 → screen now shows 36.40km (`testPlanWeekTotalDistanceMatchesRealWorkoutDistances`). (2) Profile showed the audit's literal "11-week streak" vs Today's "11 day streak" — demo fixture carried two contradictory values (`RunSmartPreviewData` streak "11-week streak" vs "11"); unified to "11 days" → both surfaces render "11 day streak". **Smoke evidence (iPhone 17 sim, Debug 1.0.9 (23)):** `docs/qa/reports/assets-2026-07-14-v109-smoke/` — 01 sign-in (daily-answer pill leads, Terms/Privacy in-app buttons), 02 Coaching step (title/CTA renamed, Back chevron, no Garmin/Rookie marketing), 03 Today (11 day streak, plan-aware Week 4 pill, honest 8 x 400m), 04 Report/Plan/Profile, 05 Goal step (Back hidden at step 0, 5 visible options). **Validation:** full suite **273 tests, 0 failures**; Debug build SUCCEEDED; binary Info.plist confirms 1.0.9 (23).

**WP-44 S1–S6 + WP-45 remainder — FTUX upgrade completion (P1 polish + instrumentation), 2026-07-14:** All six WP-44 stories and the WP-45 event remainder implemented on branch `claude/device-smoke-release-prep-50eafe`, one commit per story batch. **S1 (copy only, founder decision 2026-07-14; preview-vs-reorder structure deferred to Experiment E2):** sign-in pills now lead with "Know exactly what to run today" via testable `SignInView.featurePills`; feature-speak and compliance-speak bullets removed. **S6:** Terms/Privacy present in-app via new `Features/Shared/SafariView.swift` (`SFSafariViewController`); destinations stay `ExternalURLs`. **S4:** "Privacy" step → "Coaching", "Confirm Privacy" → "Continue" (testable statics), visible Back chevron beside the progress capsules, Garmin `DevicePreviewRow` + `RookieChallengeCallout` removed from onboarding (both already exist post-activation: Profile Garmin tile, Today `ChallengeProgressCard`); analytics step name stays `"privacy"` for funnel continuity. **S2:** `connectHealthKit` failure branch via `OnboardingHealthKitStep.failureMessage(for:)` ("Couldn't connect Apple Health. You can try again now, or later from Profile."); Report tab shows redacted skeleton rows during initial load instead of false "No verified runs yet"; `PostRunLearningCard` duplicate disabled button removed (`.unavailable` renders message only); Generate Report working/failure states verified already present. **S5:** `TodayResolvedState.restDayGuidance` (recovery actions) rendered as a HOW TO RECOVER block on rest days — the daily question always has an answer. **S3 (founder-approved design: stateless pure helper `Services/TrainingMetrics.swift`):** single accessors for week number (clamped), streak (`canonicalStreakLabel` — kills "12 days day streak" and cadence-as-streak), weekly distance (one summation), `effortLabel(for:)` collapsing THREE independent kind→zone mappers into one effort-word vocabulary, `hrvTrendGoodness` (HRV no longer hard-tinted alarm), pace labels distinguished ("Avg Pace" post-run vs "Current Pace" live). **WP-45 remainder:** `onboarding_step_abandoned`, `permission_requested/granted/denied` (location + notifications, prompt-gated), `healthkit_connect_failed`, `first_workout_viewed` (once per install), `run_report_generate_tapped/succeeded/failed` (both paths), `insight_expanded`, `share_progress_tapped` (`share_progress_completed` intentionally unwired — ShareLink has no completion API; needs UIActivityViewController migration), person property `onboarding_completed_at` via `$set`, and `plan_generation_timed_out` finally wired to the 45s activation poll. **Validation:** full suite **268 tests, 0 failures** (`-parallel-testing-enabled NO`, iPhone 17 sim); every story added focused tests (12 new) incl. the plan-required `testHealthKitConnectSurfacesFailureState`, `testTodayRendersRestDayGuidanceWhenNoWorkout`, `testWeeklyDistanceMatchesSummedWorkouts`, `testStreakUnitConsistentAcrossSurfaces`, and WP-45 payload-key tests; stale `"Zone 3"` pin in `testTodayWorkoutDisplayFallsBackToLaunchFriendlyLabels` updated to the single-source label.

**Device smoke waived — S6 empty-goal + S1 failed-card, 2026-07-15:** Founder explicitly waived the two remaining device-only smoke items (S6 empty-goal disabled Continue on a fresh install; S1 plan-generation failed card via airplane-mode-after-generating) and instructed to treat them as passing so the release could proceed. **Neither was device-observed.** Coverage instead: S6 by `testOnboardingRequiresVisibleGoalSelection` + `testOnboardingDefaultGoalIsNotHiddenValue` (red→green predicate tests on empty/hidden/visible goal values) and code review of the step-switch replacing the old paged TabView; S1 by `testPlanGenerationStateTransitionsGeneratingToReady` + `testPlanGenerationFailureExposesInlineRetry` (red→green state-machine tests) plus simulator confirmation that the `.idle` state renders nothing. If either regresses on a real device post-release, both defects are localized: S6 → `OnboardingView.canAdvanceFromGoal` / the `switch step` render; S1 → `PlanGenerationStore.apply(_:)` / `PlanGenerationStatusCard`.

Status: **1.0.9 (23) ARCHIVED AND SUBMITTED TO APP STORE CONNECT for review (2026-07-15, founder-confirmed).** Zero-streak release blocker fixed and merged (`3186343`, PR #92); 275 tests green; Release build SUCCEEDED from the main checkout at `3186343`. FTUX plan (WP-43+44+45) fully merged to main (PR #88 `5fa2e00`, PR #89 `6c6162b`, PR #92, PR #93). The live App Store build is still 1.0.8 (22) until Apple approves 1.0.9 (23).
Current Phase: PHASE 3 — FTUX upgrade: implementation + review complete; 1.0.9 (23) submitted, awaiting App Review.
Active Story: None in flight. Waiting on App Store Connect / App Review for 1.0.9 (23).
Last Completed Story: 2026-07-15 — 1.0.9 (23) archived and submitted to ASC; zero-streak Profile fix (PR #92); device smoke on S6/S1 waived by founder decision; FTUX audit board status refresh (12 of 16 findings closed, all Fixed badges verified live).
Next Recommended Story: Once 1.0.9 (23) is approved and live: verify WP-43/45 events firing in PostHog for real users, then Experiment E1 (coach preview). If App Review flags S6 or S1 (the waived items), they are the first place to look. Known analytics semantics to remember when reading funnels: onboarding_step_abandoned fires on any backgrounding; plan_generation_timed_out duration inflates if backgrounded mid-poll.
Blockers: None — waiting on Apple App Review turnaround.
Last Validation: 2026-07-15 — full suite 275 tests, 0 failures; Release-config build SUCCEEDED at `3186343`. S6/S1 device smoke explicitly waived, not observed — see entry above.
Last Updated: 2026-07-20

---

## 2026-07-13 — Public 1.0.8 (22) live smoke

| Field | Result |
|---|---|
| **Install identity** | PASS — `com.runsmart.lite`, 1.0.8 (22), public App Store binary |
| **Today Apple Health data** | PASS — Apple Health, 1.8k steps, 46 active kcal, honest missing sleep |
| **HealthKit connection/disclosure** | PASS — connected; enabled categories and read/write explanation visible |
| **Run entry** | PASS — GPS ready, Free Run, Start Run CTA |
| **Short run + completion** | PASS — live recording, two-path finish alert, review-only activity saved |
| **Fresh-account onboarding / first-run state** | NOT OBSERVED — existing signed-in account and prior history restored after reinstall |
| **Evidence** | `docs/qa/reports/release-1.0.8-build22-public-smoke-2026-07-13.md` |

---

## 2026-07-13 — RunSmart 1.0.8 (22) approved and live

| Field | Value |
|---|---|
| **Version** | 1.0.8 |
| **Build number** | 22 |
| **Approval/live date** | 2026-07-13 (founder-confirmed) |
| **Includes** | WP-37 run reliability, WP-38 run UX (splits, Live Activity), WP-40 HealthKit onboarding + auto-import |
| **ASC status** | **Approved — live on the App Store** |
| **Handoff** | `docs/qa/reports/release-1.0.8-build22-handoff.md` |
| **Replaces** | 1.0.7 (21) on ASC (~2026-07-05) |

---

## 2026-07-12 — RunSmart 1.0.8 (22) archived

| Field | Value |
|---|---|
| **Version** | 1.0.8 |
| **Build number** | 22 |
| **Archive date (UTC+3)** | 2026-07-12 |
| **Includes** | WP-37 run reliability, WP-38 run UX (splits, Live Activity), WP-40 HealthKit onboarding + auto-import |
| **ASC status** | Archived locally; waiting for review (superseded — see 2026-07-13 entry above) |
| **Handoff** | `docs/qa/reports/release-1.0.8-build22-handoff.md` |
| **Replaces** | 1.0.7 (21) on ASC (~2026-07-05) |

---

## 2026-07-05 — WP-15 build 21 submitted to App Store Connect

| Field | Value |
|---|---|
| **Version** | 1.0.7 |
| **Build number** | 21 |
| **Fix commit** | `6ed8b97` (WP-15 first-run sheet poll) |
| **Submission date (UTC+3)** | 2026-07-05 ~20:18 |
| **Upload method** | `xcodebuild -exportArchive` + `ExportOptionsAppStoreUpload.plist` |
| **Archive path** | `/tmp/runsmart-wp15-release-1783271110/build/RunSmart-v1.0.7-build21-WP15-AppStore-20260705.xcarchive` |
| **ASC status** | Upload succeeded; package processing |

**Pre-submission checklist:** `tasks/lessons.md` + `docs/qa/testflight-checklist.md` + `docs/qa/app-store-readiness-checklist.md` reviewed. Secrets present (`RunSmartSecrets.xcconfig`). First archive from dirty main worktree failed on duplicate Finder `* 2.swift` compile crash; retried from clean detached worktree (no duplicates). DerivedData cleaned before retry.

**Build bump:** `CURRENT_PROJECT_VERSION` 20 → 21 (local/uncommitted on main worktree `project.pbxproj`).

---

## WP-15 activation readout — D7 baseline vs build 21 (in progress)

### PostHog query (project 171597 — Running coach)

Tool: PostHog MCP `query-funnel` after `switch-project` → **171597**.

```json
{
  "kind": "FunnelsQuery",
  "series": [
    { "kind": "EventsNode", "event": "plan_generated" },
    { "kind": "EventsNode", "event": "plan_run_cta_tapped" },
    { "kind": "EventsNode", "event": "run_started" },
    { "kind": "EventsNode", "event": "run_completed" }
  ],
  "dateRange": { "date_from": "2026-06-19", "date_to": "2026-07-05" },
  "funnelsFilter": {
    "funnelOrderType": "ordered",
    "funnelVizType": "steps",
    "funnelWindowInterval": 7,
    "funnelWindowIntervalUnit": "day"
  },
  "filterTestAccounts": true
}
```

Supporting diagnostic (confirms sheet skip):

```json
{
  "kind": "FunnelsQuery",
  "series": [
    { "kind": "EventsNode", "event": "plan_generated" },
    { "kind": "EventsNode", "event": "first_run_cta_viewed" },
    { "kind": "EventsNode", "event": "first_run_cta_tapped" },
    { "kind": "EventsNode", "event": "run_started" },
    { "kind": "EventsNode", "event": "run_completed" }
  ],
  "dateRange": { "date_from": "2026-06-19", "date_to": "2026-07-05" },
  "funnelsFilter": { "funnelOrderType": "ordered", "funnelWindowInterval": 7, "funnelWindowIntervalUnit": "day" },
  "filterTestAccounts": true
}
```

### Before (pre-fix baseline — D7 Readout #2 + PostHog 2026-06-19→2026-07-05)

| Metric | D7 Readout #2 (2026-07-05) | PostHog funnel (same window, test accounts excluded) |
|---|---|---|
| Mature cohort `run_completed` (7d) | **0 / 12** | 0 users at `run_completed` |
| Onboarding drop | **94.7%** | (separate funnel; not WP-15 chain) |
| `plan_generated` | some reached plan | **1** user |
| `plan_run_cta_tapped` | — | **0** (0%) |
| `run_started` | — | **0** (0%) |
| `run_completed` | **0** | **0** (0%) |
| **Plan → run conversion** | **0%** | **0%** (0/1) |
| `first_run_cta_viewed` | — | **0** (sheet never shown — matches race root cause) |

**Interpretation (pre-fix):** Users who generated a plan never saw the first-run activation sheet (`first_run_cta_viewed`=0) because `presentFirstRunActivationIfNeeded` ran before async `regenerateTrainingPlan` finished. No bridge CTA, no run start. The 94.7% onboarding drop is a **separate upstream wall** (sign-in + 5 onboarding steps + 2 aha screens).

### After (build 21 cohort — pending)

| Metric | Value |
|---|---|
| Submission date | 2026-07-05 |
| Earliest readout date | **2026-07-08** (3+ days live usage) |
| Same-day PostHog (2026-07-05 only) | No data (expected — upload just completed) |
| Plan → run conversion | **TBD** — re-run query with `date_from: 2026-07-05`, filter to build 21 installs when `$app_version` / build property visible |

**WP-15 completion gate:** >=20% `plan_generated` → `run_completed` (via `plan_run_cta_tapped` + `run_started`) on next usable build-21 cohort.

### If the wall persists after build 21 (next likely causes, with evidence)

1. **Onboarding attrition (94.7%)** — still upstream; fix does not shorten onboarding. Users never reach `plan_generated`.
2. **Today `upNext` routing** — if first workout is not today, user may land on Today without obvious run CTA even after sheet fix (partially addressed PR #62; verify on device).
3. **Location / HealthKit denial at run start** — not instrumented; secondary for users who reach CTA but fail GPS/Health gate.
4. **Funnel timing** — `onboarding_completed` fires before async plan save finishes; can skew step-to-step timing in dashboards (instrumentation nuance, not the sheet race).

**Verdict (2026-07-05):** Fix shipped; **too early to measure post-fix lift**. Pre-fix PostHog confirms 0% plan-to-run and 0 `first_run_cta_viewed`, consistent with the diagnosed race. Re-query **2026-07-08+**.

**1.0.9 post-live activation verification package, 2026-07-15:** Created an isolated documentation-only worktree and executable post-approval runbook. Verified Portfolio HQ's physical-install → any qualifying `run_completed` within mature D7 decision metric, with the ordered eight-step path retained as diagnostic-only validation; also verified person-stable emulator/TestFlight/sideload/founder exclusions, every proposed WP-43/WP-45 event/property against `main`, and the operational dashboard's different rolling windows. Read-only PostHog inspection found pre-live build-23 QA telemetry (mechanics only, excluded from decisions). Live HogQL validation established `windowFunnel(604800)` with `toDateTime(timestamp)`. `share_progress_completed` is intentionally absent; `insight_expanded` is workout-breakdown-only. Product interpretation waits for ≥10 clean mature installs; E1 remains off pending its start contract and later requires ≥200 mature users/arm, ≥10% relative lift, and stable guardrails. Runbook: `docs/qa/reports/release-1.0.9-build23-post-live-activation-verification.md`.
