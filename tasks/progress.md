Status: v1.0.6 (19) is LIVE on the App Store (2026-07-01 morning). Same day, Marc Lussi rejected the Gate-4 evidence a 3rd time: "Garmin Wellness" naming and the Garmin Connect logo are both still non-compliant, and he told the team to "start all over." Root-caused and fixed on branch `fix/garmin-3rd-rejection-wellness-rename-and-logo-clip` (PR #69): renamed the feature away from "Garmin Wellness" everywhere, and removed a `.clipShape` that was altering the official Garmin Connect tile on render.
Current Phase: PHASE 2 — Garmin Production Gate 4 remediation, 3rd rejection cycle.
Active Story: Land PR #69, ship a new build with the fixes, recapture and re-verify all 6 Gate-4 screenshots on a real device, clarify Marc's "start all over" instruction before resubmitting.
Last Completed Story: 2026-07-01 - Root-caused the 3rd rejection by direct code read (not guessing): "Garmin Wellness" is an invented compound feature name that needed full removal (`GarminWellnessViews`/`.garminWellness` -> `WellnessTrendsView`/`.wellnessTrends` across `SecondaryFlowView.swift`, `ProfileTabView.swift`, `RunSmartGate4ScreenshotMode.swift`); the official Garmin Connect tile was being reshaped via `.clipShape(RoundedRectangle(...))` in two places (`GarminConnectBrandMark.swift`, `ProfileTabView`'s `ConnectedServiceTile`) which brand guidelines prohibit. Build PASS; full test suite run, one pre-existing unrelated failure confirmed via `git stash` against `main`.
Next Recommended Story: Founder merges PR #69, bumps version/build, archives + uploads + confirms live, then recaptures all 6 Gate-4 screenshots on a real device against that build, re-verifies against the brand PDF, and either replies to Marc's ticket (213145/213165) or first asks him to clarify what "start all over" means.
Estimated Completion: Code fix is done and PR'd. Remaining: founder-only App Store steps (archive/upload/confirm live), on-device screenshot recapture, brand-PDF re-verification, decision on how to interpret "start all over," then reply.
Blockers: (1) Whether `GarminConnectTile.imageset/garmin-connect-tile.jpg` (a 512x512 lossy JPEG) is actually the pristine official asset from Garmin's brand kit or a re-saved capture is unverified — worth re-downloading from `developer.garmin.com/brand-guidelines/connect/` before the next resubmission. (2) Archive/upload/submit to App Store Connect is a founder-only manual Xcode step with no git-visible commit. (3) "Generate a production app to start all over" is ambiguous (new Developer Portal production app vs. full UX rebuild) and unconfirmed with Garmin.
Last Validation: 2026-07-01 - `xcodebuild build` PASS on iPhone 17 simulator destination. Full `xcodebuild test` run; one failure (`RunSmartReadinessTests.testRunRecorderDiscardResetsCurrentWorkoutWithoutSaving`, GPS/RunRecorder, unrelated to Garmin) confirmed pre-existing on `main` via `git stash` + re-run before these changes, so not a regression from this fix.
PM Artifacts: iOS PR #69 https://github.com/nadavyigal/IOS-runsmart-light-app-/pull/69; web docs PR #109 https://github.com/nadavyigal/Running-coach-/pull/109 (GARMIN-STATUS.md updated with the 3rd-rejection root cause and status)
Last Updated: 2026-07-01
